/** @type {import('tailwindcss').Config} */
export const content = [
  "./templates/**/*.html", // Adjust the path based on where your HTML files are located
];
export const theme = {
  extend: {},
};
export const plugins = [];
